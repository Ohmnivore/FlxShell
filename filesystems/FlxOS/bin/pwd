usage = ["pwd usage: pwd returns the shell's current working directory which can be modified by the cd command", "", ""];

if (Args.length > 0)
{
    return usage;
}

return [Shell.curDir.path, "", ""];